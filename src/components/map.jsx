import React from "react";

function Map({ locationData }) {
    return (
        <>
            <div>
                <h1>{locationData}</h1>
            </div>

        </>
    )
}

export default Map